# Multi-Seed RL Evaluation Report

## Overview
Evaluated IQL policy across 5 independent seeds (0-4) with 100 episodes each to assess robustness.

## Results

(Results auto-generated by running `python scripts/run_multiseed_rl.py`)

## Stability Assessment
- Coefficient of Variation (CV): (auto-generated)
- Stability: (auto-generated)

## Methodology
1. Run IQL policy evaluation for each seed (100 episodes)
2. Compute per-seed mean and std
3. Bootstrap 95% CI across seed means (10,000 resamples)
4. Report CV = std/mean as stability metric

## How to Reproduce
```bash
python scripts/run_multiseed_rl.py
```

## Limitations
- Evaluation uses the calibrated simulator, not real deployment.
- Only IQL evaluated (not other RL algorithms).
- Single simulator configuration (no sensitivity across simulator params).
